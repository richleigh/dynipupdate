# All configuration variables use the BEES_IP_UPDATE_ prefix
# This helps avoid conflicts with other applications and makes configuration debugging easier

# CloudFlare API Configuration
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
BEES_IP_UPDATE_CF_API_TOKEN=your_cloudflare_api_token_here

# CloudFlare Zone ID
# Find this in your domain's overview page on CloudFlare dashboard
BEES_IP_UPDATE_CF_ZONE_ID=your_zone_id_here

# DNS Record Names
# Specify the EXACT full domain names you want created
# All domains are optional - configure only what you need

# Specific purpose domains (explicit full domain names)
BEES_IP_UPDATE_INTERNAL_DOMAIN=anubis.i.4.bees.wtf   # Internal/private IPs only (RFC1918)
BEES_IP_UPDATE_EXTERNAL_DOMAIN=anubis.e.4.bees.wtf   # External IPv4 only
BEES_IP_UPDATE_IPV6_DOMAIN=anubis.6.bees.wtf         # External IPv6 only

# Custom IP Ranges - Define your own VPN/private network ranges
# Supports up to 20 IPv4 and 20 IPv6 custom ranges
# Format: BEES_IP_UPDATE_IPV4_RANGE_N=<CIDR> and BEES_IP_UPDATE_IPV4_RANGE_N_DOMAIN=<domain>
# Examples:
#BEES_IP_UPDATE_IPV4_RANGE_1=100.64.0.0/10          # Tailscale CGNAT range
#BEES_IP_UPDATE_IPV4_RANGE_1_DOMAIN=anubis.ts.bees.wtf
#BEES_IP_UPDATE_IPV4_RANGE_2=10.20.0.0/16           # WireGuard VPN
#BEES_IP_UPDATE_IPV4_RANGE_2_DOMAIN=anubis.wg.bees.wtf
#BEES_IP_UPDATE_IPV6_RANGE_1=fd00::/8               # Unique Local Addresses
#BEES_IP_UPDATE_IPV6_RANGE_1_DOMAIN=anubis.vpn6.bees.wtf

# Custom IP Ranges - Define your own VPN/private network ranges
# Supports up to 20 IPv4 and 20 IPv6 custom ranges
# Format: IPV4_RANGE_N=<CIDR> and IPV4_RANGE_N_DOMAIN=<domain>
# Examples:
#IPV4_RANGE_1=100.64.0.0/10          # Tailscale CGNAT range
#IPV4_RANGE_1_DOMAIN=anubis.ts.bees.wtf
#IPV4_RANGE_2=10.20.0.0/16           # WireGuard VPN
#IPV4_RANGE_2_DOMAIN=anubis.wg.bees.wtf
#IPV6_RANGE_1=fd00::/8               # Unique Local Addresses
#IPV6_RANGE_1_DOMAIN=anubis.vpn6.bees.wtf

# Combined domain - THE MAIN FEATURE!
# This domain gets ALL IPs (internal + custom ranges + external IPv4 + IPv6)
# Use this as your primary hostname - it works from anywhere:
#   - From internal network: resolves to internal IPs (192.168.x.x, 10.x.x.x)
#   - From custom VPN ranges: resolves to your configured range IPs
#   - From internet: resolves to external IPv4/IPv6
#   - DNS returns all records, your OS picks the best route
BEES_IP_UPDATE_COMBINED_DOMAIN=anubis.bees.wtf

# Top-level CNAME alias (OPTIONAL)
# Creates a friendly CNAME pointing to COMBINED_DOMAIN
# Example: anubis.example.com -> anubis.bees.wtf
# Users can use the memorable name, DNS resolves through CNAME to get all IPs
BEES_IP_UPDATE_TOP_LEVEL_DOMAIN=anubis.example.com

# Optional: Whether to proxy records through CloudFlare (default: false)
# Set to 'true' to enable CloudFlare proxy (orange cloud)
# Note: Typically set to false for dynamic DNS
BEES_IP_UPDATE_CF_PROXIED=false

# Cleanup Configuration (only used when running with -cleanup flag)
# How old a heartbeat must be before records are considered stale
BEES_IP_UPDATE_STALE_THRESHOLD_SECONDS=3600   # 1 hour (default)

# How often to check for stale records
BEES_IP_UPDATE_CLEANUP_INTERVAL_SECONDS=300   # 5 minutes (default)
