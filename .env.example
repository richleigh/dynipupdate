# CloudFlare API Configuration
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
CF_API_TOKEN=your_cloudflare_api_token_here

# CloudFlare Zone ID
# Find this in your domain's overview page on CloudFlare dashboard
CF_ZONE_ID=your_zone_id_here

# Instance identifier (used in heartbeat records)
# Defaults to machine hostname if not set
INSTANCE_ID=my-server-name

# DNS Record Names (REQUIRED)
# IMPORTANT: Do NOT include your zone name (e.g., .bees.wtf)
# CloudFlare automatically appends the zone name to these subdomains
#
# Examples for zone "bees.wtf":
#   INTERNAL_DOMAIN=anubis.i.4          creates: anubis.i.4.bees.wtf
#   EXTERNAL_DOMAIN=anubis.e.4          creates: anubis.e.4.bees.wtf
#   IPV6_DOMAIN=anubis.6                creates: anubis.6.bees.wtf
#   COMBINED_DOMAIN=anubis              creates: anubis.bees.wtf

# Specific purpose domains
INTERNAL_DOMAIN=host.internal.example.com  # For internal/private IPs only
EXTERNAL_DOMAIN=host.external.example.com  # For external IPv4 only
IPV6_DOMAIN=host.ipv6.example.com          # For external IPv6 only

# Combined domain - THE MAIN FEATURE!
# This domain resolves to ALL IPs (internal + external IPv4 + IPv6)
# Use this as your primary hostname - it works from anywhere:
#   - From internal network: resolves to internal IPs (192.168.x.x, 10.x.x.x)
#   - From internet: resolves to external IPv4/IPv6
#   - DNS returns all records, your OS picks the best route
COMBINED_DOMAIN=host.example.com

# Optional: Whether to proxy records through CloudFlare (default: false)
# Set to 'true' to enable CloudFlare proxy (orange cloud)
# Note: Typically set to false for dynamic DNS
CF_PROXIED=false
