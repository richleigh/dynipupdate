# CloudFlare API Configuration
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
CF_API_TOKEN=your_cloudflare_api_token_here

# CloudFlare Zone ID
# Find this in your domain's overview page on CloudFlare dashboard
CF_ZONE_ID=your_zone_id_here

# Base hostname (required)
# This will be used if specific domains are not provided
HOSTNAME=myhost.example.com

# Optional: Specific domain/subdomain for internal IPv4 address
# If not set, HOSTNAME will be used
INTERNAL_DOMAIN=internal.myhost.example.com

# Optional: Specific domain/subdomain for external IPv4 address
# If not set, HOSTNAME will be used
EXTERNAL_DOMAIN=myhost.example.com

# Optional: Specific domain/subdomain for external IPv6 address
# If not set, HOSTNAME will be used
IPV6_DOMAIN=ipv6.myhost.example.com

# Optional: Whether to proxy records through CloudFlare (default: false)
# Set to 'true' to enable CloudFlare proxy (orange cloud)
# Note: Typically set to false for dynamic DNS
CF_PROXIED=false
